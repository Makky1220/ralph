# ralph.toml — project-level configuration for the ralph CLI.
# Generated by `ralph init`. Edit freely.

[doctor]
# Fail `ralph doctor` when the claude binary is missing.
require_claude_cli = true
# Fail `ralph doctor` when go is missing (set true for Go projects).
require_go = false
